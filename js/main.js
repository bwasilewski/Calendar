(function ($) {

    $('.jquery-calendar-wrap').calendar({
        message: 'Message override!',
        title: 'jQuery Calendar',
        previous: '#apervita-previous',
        next: '#apervita-next'
    });

    $('.jquery-calendar-wrap-clone').calendar({
        message: 'Message override!',
        title: 'jQuery Calendar Clone',
        // previous: '#apervita-previous',
        // next: '#apervita-next'
    });

}(jQuery));
